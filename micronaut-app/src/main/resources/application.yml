micronaut:
  server:
    port: 8080

datasources:
  default:
    db-type: postgres
    dialect: "POSTGRES"
    driverClassName: "org.postgresql.Driver"

flyway:
  datasources:
    default:
      enabled: true

greeting:
  name: "Bitelchus"
